---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<section class={`section manifesto-hero ${className}`}>
    <div class="container container--narrow">
        <div class="manifesto-header">
            <span class="manifesto-badge reveal">Nuestro Manifiesto</span>
            <div class="manifesto-icon reveal" aria-hidden="true">
                <i class="ph-duotone ph-lightning"></i>
            </div>
        </div>
        <div class="manifesto-content">
            <h1 class="manifesto-text reveal">
                Lo blando no <span class="highlight">convierte</span>.<br />
                Lo tibio no <span class="highlight">escala</span>.<br />
                El diseño sin datos es <span class="highlight">arte</span>.
            </h1>
            <div class="manifesto-divider reveal"></div>
            <p class="manifesto-intro reveal">
                Zutra nació para los que van a todas. Construimos
                <strong>activos digitales que mueven la aguja</strong>,
                no ruido que se pierde en el timeline.
            </p>
            <div class="manifesto-stats reveal">
                <div class="stat-item">
                    <span class="stat-number">0%</span>
                    <span class="stat-label">Blandenguería</span>
                </div>
                <div class="stat-item">
                    <span class="stat-number">100%</span>
                    <span class="stat-label">Ejecución</span>
                </div>
                <div class="stat-item">
                    <span class="stat-number">∞</span>
                    <span class="stat-label">ROI</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .manifesto-hero {
        padding: clamp(6rem, 12vh, 8rem) 0 clamp(4rem, 10vh, 6rem);
        position: relative;
        text-align: center;
    }

    .manifesto-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 2.5rem;
    }

    .manifesto-badge {
        display: inline-block;
        background: var(--color-accent);
        color: var(--color-on-accent);
        padding: 0.5rem 1rem;
        border-radius: var(--radius-full);
        font-size: 0.75rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        position: relative;
        z-index: 1;
    }

    .manifesto-icon {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 40px;
        height: 40px;
        opacity: 0.6;
        transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .manifesto-icon:hover {
        transform: scale(1.05) rotate(5deg);
        opacity: 0.9;
    }

    .manifesto-icon i {
        font-size: 1.8rem;
        color: var(--color-accent);
    }

    .manifesto-content {
        position: relative;
        z-index: 1;
        max-width: 800px;
        margin: 0 auto;
    }

    .manifesto-text {
        font-size: clamp(
            2.2rem,
            5vw,
            3.5rem
        );
        font-weight: 800;
        line-height: 1.2;
        color: var(--color-secondary);
        letter-spacing: -0.02em;
        margin-bottom: 1.5rem;
        text-align: center;
    }

    .highlight {
        color: var(--color-accent);
        font-weight: 900;
        position: relative;
        display: inline-block;
    }

    .highlight::after {
        content: '';
        position: absolute;
        bottom: -1px;
        left: 0;
        right: 0;
        height: 2px;
        background: var(--color-accent);
        border-radius: 1px;
        transform: scaleX(0);
        transform-origin: left;
        animation: highlightUnderline 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94) 0.5s forwards;
    }

    @keyframes highlightUnderline {
        to {
            transform: scaleX(1);
        }
    }

    .manifesto-divider {
        width: 60px;
        height: 2px;
        background: linear-gradient(90deg, var(--color-accent), var(--color-secondary));
        border-radius: 1px;
        margin: 2rem auto;
        opacity: 0;
        transform: scaleX(0);
        animation: dividerIn 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94) 0.7s forwards;
    }

    @keyframes dividerIn {
        to {
            opacity: 1;
            transform: scaleX(1);
        }
    }

    .manifesto-intro {
        font-size: clamp(1rem, 1.8vw, 1.15rem);
        line-height: 1.6;
        color: var(--color-muted);
        max-width: 500px;
        margin: 0 auto 2.5rem;
        text-align: center;
        opacity: 0;
        transform: translateY(10px);
        animation: fadeInUp 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94) 0.9s forwards;
    }

    .manifesto-intro strong {
        color: var(--color-secondary);
        font-weight: 600;
    }

    @keyframes fadeInUp {
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .manifesto-stats {
        display: flex;
        gap: 2.5rem;
        justify-content: center;
        margin-top: 1.5rem;
        opacity: 0;
        transform: translateY(15px);
        animation: fadeInUp 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94) 1.1s forwards;
    }

    .stat-item {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
    }

    .stat-number {
        font-size: clamp(1.5rem, 3vw, 2rem);
        font-weight: 800;
        color: var(--color-accent);
        line-height: 1;
        margin-bottom: 0.4rem;
        display: block;
    }

    .stat-label {
        font-size: 0.75rem;
        font-weight: 500;
        color: var(--color-muted);
        text-transform: uppercase;
        letter-spacing: 0.04em;
    }

    @media (max-width: 860px) {
        .manifesto-header {
            flex-direction: column;
            align-items: flex-start;
            gap: 1rem;
        }

        .manifesto-stats {
            gap: 2rem;
        }
    }

    @media (max-width: 640px) {
        .manifesto-hero {
            padding: clamp(4rem, 10vh, 6rem) 0 clamp(3rem, 8vh, 5rem);
        }

        .manifesto-text {
            font-size: clamp(2.2rem, 8vw, 3rem);
            line-height: 1.1;
        }

        .manifesto-stats {
            flex-direction: column;
            gap: 1.5rem;
            align-items: center;
        }

        .manifesto-intro {
            font-size: 1rem;
        }

        .stat-number {
            font-size: clamp(1.8rem, 5vw, 2.5rem);
        }
    }
</style>
