---
import { getSanityClient, groq } from '@/lib/sanity';

const client = await getSanityClient();
const docs = await client.fetch(groq`*[_type == "testimonial"]`);
---

<html>
  <body>
    <h1>Debug Testimonials</h1>
    <pre>{JSON.stringify(docs, null, 2)}</pre>
  </body>
</html>
